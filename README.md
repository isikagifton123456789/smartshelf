# SmartShelf Monorepo

This repository is now split into separate apps:

- `frontend/` → React + Vite UI
- `backend/` → Express + Firebase API

## Run frontend

```bash
cd frontend
npm install
npm run dev
```

## Run backend

```bash
cd backend
npm install
npm run dev
```

Backend setup details are in `backend/README.md`.
